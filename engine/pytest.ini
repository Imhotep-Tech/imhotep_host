[pytest]
# Tells pytest to look in the current directory for modules (fixes the import error)
pythonpath = .

# Silences the noisy dependency warnings so your test output is clean
filterwarnings =
    ignore::UserWarning
    ignore::DeprecationWarning